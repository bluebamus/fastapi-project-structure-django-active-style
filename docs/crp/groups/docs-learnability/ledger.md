# Ledger — Docs Learnability (결함 대장)

> ID 규칙: 이 그룹의 결함은 다른 그룹과 충돌하지 않도록 **F-201** 부터 매긴다.

| ID | 등급 | 위반 계약 | 처리 | 상태 | 내용 | 조치 |
|---|---|---|---|---|---|---|
| F-201 | HIGH | REQ-001 / INV-1·INV-3 (ORM/Raw 학습 경로) | Fix | **Fixed** | 학습자용 문서 3종(`README.md`·`QUICKSTART.md`·`ARCHITECTURE.md`)에 Raw SQL·`RawRepositoryBase`·catalog·reports 언급이 **각각 0건**이었다. README 는 `workflow-guide.md` 도 `docs/project-guide/` 도 한 번도 링크하지 않았다. 즉 1,206행 지침서와 두 완결 예제가 **존재하지만 도달 불가**였다. 사용자가 명시한 목표 두 개 중 하나가 통째로 미달성 상태. | Round 1. `docs/guides/orm-raw-workflow.md` 신규 작성(선택 기준·파일 순서·자주 틀리는 지점·결과 API 의미·MySQL 검증 절차) + README 에 `## ORM / Raw 데이터 접근` 섹션과 목차 항목 추가. 회귀: `tests/test_docs_learnability.py` — fail-on-revert 확인. |
| F-202 | MED | INV-2 (저장소 정체성) | Fix | **Fixed** | README 제목이 `# FastAPI Default Project Structure` — **다른 저장소(형제 repo)의 이름**이었다. 2026-08-12 기준선 교체(`f35a938`) 때 형제 저장소 스냅샷을 들여오며 남은 잔재. 학습자가 **가장 먼저 보는 한 줄**이 틀린 상태였다. | Round 1. `# FastAPI Project Structure — Django Active Style` 로 교체. 회귀 테스트가 이전 이름의 재유입도 막는다. |
| F-203 | MED | ADR-003 / INV-5 (문서 진입점) | Fix | **Fixed** | `docs/` 에 안내 문서가 없어 5개 하위 폴더 중 무엇부터 볼지 알 수 없었다. 특히 날짜가 박힌 두 폴더 중 **하나만 지금도 유효한 지침서**라는 사실이 어디에도 없었다 — 학습자가 "지난 기록" 으로 읽고 건너뛰는 것이 합리적인 상태였다. | Round 1. `docs/README.md` 신규 작성 — 읽는 순서, 폴더별 성격(살아 있는 문서 / 설계 기준선 / 검수 이력) 구분. |
| F-204 | MED | REQ-001 (문서 현행성) | Fix | **Fixed** | `docs/project-guide/v1.0.0/08-data-runtime-workflow` 가 "Raw SQL과 ORM 구현을 선택하는 구조는 **별도 계획이며 아직 런타임 선택 기능이 아니다**" 라고 서술. 08-18 작성이라 08-19 의 Raw 계층·예제를 모른다. 학습자가 이걸 읽으면 **Raw 계층을 찾아볼 이유 자체가 사라진다** — 없는 것을 찾지는 않는다. | Round 1. 갱신 주석으로 현행화하고 가이드·두 예제를 가리키게 했다. 원 문장은 작성 시점 기록으로 남기고 그 위에 갱신을 덧붙이는 형태 — 버전 문서라 통째로 고쳐 쓰지 않는다. |
| F-205 | LOW | INV-8·INV-9 (안내 일관성) | Fix | **Fixed** | ① `pyproject.toml` 의 `name` 이 `fastapi-default-project-structure` 로 F-202 와 같은 잔재. ② 생성기가 뼈대만 만들고 끝나 "다음에 무엇을 할지" 를 알려주지 않았다 — 이 구조에서 가장 먼저 정해야 하는 것이 ORM/Raw 선택인데 그 안내가 없었다. | Round 1. 패키지명을 저장소명과 일치시키고, 생성기 출력에 가이드·두 예제 경로를 추가했다(로직 불변, 출력 문구만). |
| F-206 | LOW | REQ-001 (문서 현행성) / R-011 | Fix | **Fixed** | 원본 지침서 §2.1 은 Dependency 인자와 Service/Repository 속성을 `db_session`/`self.db_session` 으로 규정하는데 저장소 코드는 `session`/`self.session` 을 쓴다(92지점). 지침서를 따라 읽는 학습자가 코드에서 그 이름을 찾지 못한다. | Round 2. **지침서는 고치지 않았다** — ADR-001·C-3 이 원본을 읽기 전용 설계 기준선으로 못박았고, 코드 개명은 회귀 위험 대비 얻는 것이 없다(2026-08-19 사용자 결정). 실제 피해인 혼선만 닫는다: 살아 있는 가이드 §6, 지침서로 보내는 그 자리에 표기 차이와 "동작을 결정하는 것은 Dependency 함수 이름" 을 명시했다. |
| F-207 | MED | REQ-001 (문서 현행성) / INV-1 | Fix | **Fixed** | Round 1 이 08 문서 한 건만 고쳤는데, 같은 기준으로 나머지 8종을 스캔하니 ORM/Raw 를 **미구현으로 단언**하는 문장이 3건 더 있었다 — `00-document-index`(후속 고도화 계획이다), `01-system-design`(완료 기능으로 간주하지 않는다), `02-feature-catalog`(현재 기능 목록에 포함하지 않는다). F-204 와 같은 피해다: 없다고 배운 것을 찾아보지는 않는다. | Round 2. 원문은 남기고 `> **갱신(2026-08-20)**` 블록을 덧붙였다(F-204 선례). 회귀는 문장이 아니라 **갱신 블록의 링크**를 검사한다 — 링크가 끊기면 정정만 사라지고 틀린 원문이 남는 것이 이 형식의 실제 고장 모드다. fail-on-revert 확인. |

| F-208 | HIGH | REQ-003 / INV-1 (학습 경로의 정확성) | Fix | **Fixed** | **초기화 훅의 위치를 문서 전체가 틀리게 가르쳤다.** 코드는 이미 `app/features/<name>/apps.py` 의 `ready()` + `AppRegistry.install_hooks()` 로 옮겼고 `discover()` 는 부작용이 0 인데(ADR-006), README §「초기화 훅의 한계」·ARCHITECTURE §2.1·§2.3·concept 02 §2-3 은 전부 `__init__.py` import-time 부수효과로 설명했다. 실제 `home/__init__.py` 에는 **"여기 두지 마라"** 가 적혀 있어 문서와 코드가 정면으로 반대다. 자동 등록 골격에서 새 앱을 만드는 개발자가 **정확히 틀린 자리에 훅을 넣는다** — 그리고 그 훅은 조용히 실행되지 않는다. | Round 3. 살아 있는 문서는 직접 교체(README 규약표에 `apps.py` 행 추가 + §초기화 훅을 `ready()` 예제로 재작성, ARCHITECTURE 트리·결선 도식·규약표·main.py 예시·Django 대응표·필수선택표 6곳). 날짜 고정 문서(concept 02 §2-3, spec 03 대응표)는 원문 보존 + 갱신 블록(ADR-003 선례). |
| F-209 | MED | REQ-003 (골격 완결성) | Fix | **Fixed** | 생성기 `scripts/new_app.py` 가 `apps.py` 를 만들지도, 출력에서 언급하지도 않았다. Django `AppConfig.ready()` 대응물이 **골격 자체에서 발견 불가능**한 상태 — 생성기만 따라간 개발자는 훅의 존재를 알 수 없다. | Round 3. 모듈 독스트링 컨벤션 목록과 실행 안내 출력에 한 줄씩 추가(로직 불변, 문구만 — charter 인벤토리의 "생성기 안내" 항목). 파일을 자동 생성하지는 않는다 — 대부분의 앱에 훅이 필요 없고, 빈 `ready()` 를 뿌리면 그것대로 노이즈다. |
| F-210 | MED | ADR-004 (현행 참칭 문서) | Fix | **Fixed** | `docs/concepts/auto-discovery-registry-2026-06-25.html` 이 `create_app()`·`bootstrap` 시대 배선을 도식으로 설명하는데 `app/core/bootstrap.py` 는 **존재하지 않는다**. 더 나쁜 것은 `docs/concepts/README.md` 가 이 문서를 "이 저장소의 **현행 배선**" 이라고 소개한 점 — 학습자를 옛 모델로 능동적으로 안내한다. | Round 3. ADR-004 에 따라 `docs/concepts/` 폴더 전체 삭제(html + 그것만 가리키던 인덱스). 링크 3곳 정리 — `docs/README.md` 폴더 표 행, concept 02 참고 절. |
| F-211 | MED | ADR-004 (현행 참칭 문서) | Fix | **Fixed** | `01-project-hardening-plan-2026-08-11.md` 의 "미적용 4건은 실재하는 보안·복원력 이슈다" 상태표 중 **3건이 이미 해소**됐다 — DB 오류 detail 비노출(INV-17), lifespan `ResourceManager` try/finally, `SELECT ... FOR UPDATE` 라우팅 판별. 문서는 "기준선 교체 후 재확인한 실제 적용 여부" 라고 스스로 현재를 주장하므로 읽는 사람은 이 셋이 여전히 뚫려 있다고 믿는다. | Round 3. 삭제. 남은 1건(`TRUSTED_PROXY_IPS` 부재 — 전달 헤더 무조건 신뢰)은 유실되지 않는다: `project-guide/01-system-design` §보안 경계와 `06-home-access-log` §클라이언트 IP 판정이 같은 사실을 더 정확히 서술하고 있고 두 문서는 유지된다. 폴더 README 의 열람 순서·역할 표에서 항목 제거·번호 재조정. |
| F-212 | LOW | REQ-003 (문서 정확성) | Fix | **Fixed** | `ARCHITECTURE.md` 폴더 트리 최상단이 `fastapi-default-project-structure/` — **형제 저장소 이름**이었다(F-202 와 같은 계보의 잔재). 아키텍처 SSOT 의 첫 코드블록 첫 줄이라 눈에 가장 먼저 들어온다. | Round 3. 저장소명으로 교체. |
| F-213 | LOW | INV-10(정식 세션 이름) / REQ-003 | Fix | **Fixed** | ① `ARCHITECTURE.md` §4 코드 예시가 alias 이름(`get_session`·`get_read_session`)을 쓴다 — 정식 이름은 `*_db_session` 이고 INV-10 은 저장소 안에서 정식 이름만 쓰도록 규정한다. ② `### 3.3` 절 번호가 두 번 나온다. ③ `app/features/blog/tests/test_transaction_boundary.py` 독스트링이 존재하지 않는 `docs/review/improvement-plan-2026-08-10.md` 를 판정 근거로 인용한다. | Round 3. ①은 `get_writer_db_session`/`get_read_only_db_session` 으로 교체(문서만 — 코드 alias 는 그대로), ②는 `3.4` 로 재번호, ③은 실재하는 `docs/guides/ARCHITECTURE.md` §4 로 교체(독스트링만, 테스트 로직 불변). |

<!--
규칙:
- 계약 위반만 Fix. 나머지는 Accept-out-of-scope(→ residual-risk.md) 또는 Wont-fix.
- Fix 는 회귀 테스트 + fail-on-revert 검증 후에만 Status=Fixed.
- Open 인 Fix 가 0건이어야 GATE 5 Done.
-->
