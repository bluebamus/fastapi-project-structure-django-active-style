# Ledger — orm-raw-repository (결함 원장)

> 모든 finding 의 **영구 기록**. 한 번 적힌 항목은 사라지지 않는다(상태만 바뀐다).
> `Status=Fixed/Accepted` 인 항목은 다음 라운드에서 재검·재수정하지 않는다.
> 분류: **Fix**(계약 위반·고친다) / **Accept-out-of-scope**(→residual-risk) / **Wont-fix**(오탐).
> 심각도: CRIT / HIGH / MED / LOW.

| ID | Severity | 위반 계약 조항 | 결정 | Status | 근거(코드 위치) | 비고 |
|---|---|---|---|---|---|---|
| F-001 | HIGH | INV-5 (동일 registry 재사용) | Fix | **Fixed** | `app/core/db/session.py:227-230` 이 `import_all_models()` 로 모델을 재-discovery. `main.py:70` 의 `create_db_tables()` 호출 경로. | Phase 1 완료. `import_all_models()` 호출 제거 + 빈 metadata 거부 가드. 회귀: `tests/core/test_create_db_tables.py`(3) — 수정 전 RED 확인. |
| F-002 | MED | 2-1 지원 구성(PK 타입 보존) | Fix | **Fixed** | `app/core/repositories/crud_base.py:59` — `session.get(self.model, str(id))` 로 PK 를 문자열 강제 변환. | Phase 3 완료. `CRUDBase[ModelType, PrimaryKeyType]`(PEP 696 기본값 str) + `pk_attr` + `_pk` 도입, `_get()` 은 PK 를 그대로 전달. `BaseRepository` 의 `id: str` 10곳도 `PrimaryKeyType` 으로 관통. 회귀: `tests/core/test_pk_generic.py`(9, str/int/UUID/tuple 보존). |
| F-003 | HIGH | INV-4 (설정 무관 read-only 보장) | Fix | **Fixed** | 가드가 `app/core/db/router.py:141-147` 의 `RoutingSession.get_bind()` 안에만 존재. `session.py:147`(라우터 on) 만 이 클래스를 쓰고 `:149`(라우터 off) 는 평범한 `async_sessionmaker` 를 쓴다. `:184` `BackgroundSessionLocal` 도 항상 비보호. | Phase 2 완료. 집행 지점을 `RoutingSession.get_bind()` 에서 **`Session` 클래스 이벤트**(`before_flush` / `do_orm_execute`)로 옮겨 sessionmaker·엔진과 무관하게 적용. 중앙 `is_read_only()`/`assert_writable()` 도입, Raw SQL 은 default-deny 판별. 회귀: `tests/core/test_read_only_guard.py`(52, 라우터 on/off parameterize). |
| F-004 | MED | INV-8 (OpenAPI 스키마명) | Fix | **Fixed** | component schemas 31개 중 2개가 모듈 경로형: `app__features__auth__schemas__auth_schema__UserResponse`, `app__features__user__schemas__user_schema__UserResponse`. | Phase 9. auth 쪽을 `AuthUserResponse` 로 개명(두 DTO 는 필드도 다르다 — 인증용은 시각 정보를 노출하지 않는다). 회귀: `tests/test_openapi_contract.py` 가 component key 의 `__` 를 전면 금지한다 — 개명이 아니라 **충돌 자체**를 막는다. fail-on-revert 확인. |
| F-005 | LOW | INV-8 (tag 일관성) | Fix | **Fixed** | `tags_metadata` 선언 = `[Analytics, Blog, Health, Home, Reply, SNS, User]`. `Analytics` 선언·미사용, `Auth` 사용·미선언. | Phase 9. `Analytics` 제거, `Auth`/`Catalog`/`Reports` 추가, User/Blog/Reply/SNS 의 "(예정)" 설명을 현재 구현 상태로 교체. 회귀: 선언·사용 **양방향** 일치 검사 + "예정/미구현" 문구 검사. fail-on-revert 확인. |
| F-006 | HIGH | 2-2 위협 모델(무인증 Admin 의 운영 반입 차단) | Fix | **Fixed** | `config.py` 에 ENV 기반 배포 안전성 게이트가 아예 없었음(staging/production 에서 `ADMIN=true`·`DEBUG=true`·placeholder secret·와일드카드 CORS 가 전부 통과). | **Phase 0 서술 정정**: 처음에는 "authentication_backend 미주입" 을 결함으로 적었으나, 2026-08-12 에 "SQLAdmin 인증 백엔드 미부착 = 영구 비목표" 로 확정돼 있었다(ADR-005). 인증 주입은 요구사항 회귀이므로 취소하고, 올바른 방어선인 fail-fast 로 재정의해 구현했다. 회귀: `tests/core/test_deployment_safety.py`(13). |
| F-007 | MED | 2-1 지원 구성(MySQL 통합) | Fix | **Fixed** | `compose.test.yaml` 부재. `.github/workflows/ci.yml` 에 MySQL service/통합 단계 없음(pytest 단계만 존재, skip/xfail 0 게이트는 이미 있음). | Phase 5 완료. `compose.test.yaml`(mysql:8.4, 포트 3310, tmpfs, healthcheck) 신규 + `.github/workflows/ci.yml` 에 `mysql` job 추가. 로컬 WSL 컨테이너로 **실제 실행 검증**: `pytest -m mysql` 5 passed(skip 0), 전체 suite 458 passed(skip·deselect 0). 회귀: `tests/integration/test_mysql_harness.py`(5). |
| F-008 | MED | C-9 / INV-5 (발견과 결선의 분리) | Fix | **Fixed** | 기능 `__init__.py` 6개가 라우터·모델을 eager import 해, `discover()` 만으로 라우팅 트리와 DB 모델 **29개 모듈**이 메모리에 올라왔다. `home/access_log_sink.py` 는 모듈 레벨에서 `background_session`·서비스까지 끌어왔다. | Phase 1 에서 발견. init 경량화 + sink 의 DB import 를 호출 시점으로 이동. 회귀: `tests/core/test_import_boundary.py`(서브프로세스로 실제 경계 측정, 2). |
| F-009 | HIGH | charter 3 인수기준(자원 회수) | Fix | **Fixed** | `lifespan` 의 정리 코드가 `yield` **뒤에만** 있어, startup 실패(테이블 생성 실패 등) 시 `drain()`/`dispose_engine()` 에 도달하지 못했다 — 기동 재시도마다 커넥션이 누적된다. | Phase 1 에서 발견. try/finally 로 전환. 회귀: `tests/test_lifespan.py`(3) — 수정 전 실패 경로 RED 확인. |
| F-010 | MED | C-5 (로그 보존) | Fix | **Fixed** | `migrations/env.py` 의 `fileConfig()` 가 기본값 `disable_existing_loggers=True` 로 동작해 migration 이 돌 때마다 기존 앱 로거가 조용히 꺼졌다. | Phase 1 에서 발견. 플래그 명시. 회귀: `tests/core/test_alembic_logging.py`(AST 검사 + 동작 검증, 2). |
| F-011 | HIGH | 2-2 위협 모델(입력 신뢰 경계) | Fix | **Fixed** | `app/core/middlewares/user_info_middleware.py:86` 이 `X-Forwarded-For`/`X-Real-IP` 를 **무조건** 접속 IP 로 채택. 프록시 뒤가 아닌 배포에서 클라이언트가 헤더 한 줄로 IP 를 위조하고 접속 로그·IP 기반 조회를 오염시킬 수 있었다. | Phase 1-R. `TRUST_PROXY_HEADERS`(기본 false) 도입, 켠 배포에서만 헤더 채택. `.env.example` 문서화. 회귀: `tests/core/test_client_ip_trust.py`(7). |
| F-012 | MED | 2-2 위협 모델(토큰 서명 분리) | Fix | **Fixed** | access/refresh 서명 키가 동일해도 기동이 통과했다 — 같은 키면 refresh 토큰이 access 로도 검증을 통과한다. | Phase 1-R. `validate_deployment_safety()` 에 동일 키 거부 추가. 회귀: `tests/core/test_deployment_safety.py`(+2). |
| F-013 | MED | 계획서 §8 `/ready` 사양 | Fix | **Fixed** | Phase 1 구현이 사양과 4건 어긋났다: operationId `readinessCheck`(사양 `getReadiness`), 전용 `ReadyResponse`(사양 `HealthResponse` 200 / 표준 오류 503), read-only 세션(사양 **writer** `SELECT 1`), timeout 없음(사양 2초). | Phase 1-R. 4건 모두 사양대로 정렬. 회귀: `tests/test_ready.py`(8). |
| F-014 | LOW | C-5 (오류 응답·로그 비노출) | Fix | **Fixed** | 전역 예외 핸들러가 `DEBUG` 에서 `str(exc)` 를 응답에 실었고(개발 환경 예외에도 DSN·쿼리·입력값이 실린다), 로그에 raw path 를 남겨 경로 식별자가 새어나갔다. | Phase 1-R. 응답 detail 은 항상 None, 로그는 route template. 회귀: `tests/test_exception_handler_leak.py`(3) — 누출을 되돌리면 실패함을 확인(fail-on-revert). |
| F-015 | LOW | 계획서 §8 (ADMIN lazy import 검증) | Fix | **Fixed** | 코드는 이미 lazy 였으나 이를 지키는 테스트가 없어 다음 변경이 조용히 깨뜨릴 수 있었다. | Phase 1-R. 서브프로세스로 `ADMIN=false` 시 sqladmin 미적재 확인 + 대조군. 회귀: `tests/core/test_admin_lazy_import.py`(2). |
| F-016 | MED | 계획서 §8 resource manager | Fix | **Closed** | `app/core/resources.py` 부재. 현재는 lifespan try/finally 로 직접 정리하며, 자원별 context manager·멱등 cleanup 등록 구조가 없다. | **Phase 1-R2 에서 해소**(2026-08-19). 별도 작업 그룹 `docs/crp/groups/runtime-lifecycle/` 이 인수해 Round 1 에서 Fixed. 상세 내용과 회귀 테스트는 그 그룹 ledger 참조. |
| F-017 | MED | 계획서 §8 bounded logging queue | Fix | **Closed** | `QueueHandler`/`QueueListener` 미도입. 파일 logging I/O 가 event loop 안에서 일어나고, `uvicorn main:app` import 경로에서 bootstrap 이 한 번만 되는지 미검증. | **Phase 1-R2 에서 해소**(2026-08-19). 별도 작업 그룹 `docs/crp/groups/runtime-lifecycle/` 이 인수해 Round 1 에서 Fixed. 상세 내용과 회귀 테스트는 그 그룹 ledger 참조. |
| F-018 | MED | 계획서 §8 SQL noise filter | Fix | **Closed** | queue handler 의 SQL/driver DEBUG·INFO 차단(WARNING 이상 유지)과 development/test 한정 `LOG_SQL_ECHO_ENABLED` opt-in 이 없다. Phase 1 의 `RedactingFilter` 는 마스킹이지 noise 차단이 아니다. | **Phase 1-R2 에서 해소**(2026-08-19). 별도 작업 그룹 `docs/crp/groups/runtime-lifecycle/` 이 인수해 Round 1 에서 Fixed. 상세 내용과 회귀 테스트는 그 그룹 ledger 참조. |
| F-019 | MED | 계획서 §8 Celery 생명주기 | Fix | **Closed** | prefork `worker_process_init`/`worker_process_shutdown` 에서 child engine/loop 생성·부모 pool 폐기·멱등 정리가 없다. 개발 startup DDL 을 단일 worker 로 제한하는 장치도 없다. | **Phase 1-R2 에서 해소**(2026-08-19). 별도 작업 그룹 `docs/crp/groups/runtime-lifecycle/` 이 인수해 Round 1 에서 Fixed. 상세 내용과 회귀 테스트는 그 그룹 ledger 참조. |
| F-020 | LOW | C-9 (발견 단계 부작용) | Fix | **Closed** | `home/__init__.py` 의 import-time `register_sink()` 가 남아 있다. 계획서 §3 은 "제거하거나 명시적 멱등 init hook 으로 이동" 을 요구한다. | **Phase 1-R2 에서 해소**(2026-08-19). 별도 작업 그룹 `docs/crp/groups/runtime-lifecycle/` 이 인수해 Round 1 에서 Fixed. 상세 내용과 회귀 테스트는 그 그룹 ledger 참조. |
| F-021 | MED | §4 입력 불변성 / PK 소유권 | Fix | **Fixed** | `create()` 가 호출자 dict 에 `data["id"] = str(uuid4())` 를 직접 써넣었다. 호출자 자료구조를 바꾸는 부작용이자, PK 생성 책임을 모델(mixin default)에서 Base 로 끌어온 것이다. 정수 PK·시퀀스·외부 키를 쓰는 모델에서 어긋난다. | Phase 4. 입력을 복사해 쓰고 id 주입을 제거. 회귀: `tests/core/test_repository_contract.py`. |
| F-022 | MED | §4 update/delete 계약 | Fix | **Fixed** | `update()`/`delete()` 가 bulk DML(`update().where()` / `delete().where()`)로 동작해 "없는 행" 과 "변경 없는 행" 을 구분하지 못했고, unknown 필드·PK 변경을 거르지 않았으며 빈 PATCH 의 의미도 정의돼 있지 않았다. | Phase 4. 단일 엔티티 선조회로 전환, unknown/PK 변경 거부, 빈 PATCH 는 존재 확인 후 no-op. |
| F-023 | HIGH | C-5 (오류 응답 비노출) | Fix | **Fixed** | 예외 `detail` 에 `str(e.orig)` / `str(e)` 로 **드라이버 오류 원문**을 담았고, 이 detail 은 `AppException.to_response()` 를 통해 그대로 API 응답으로 나갔다. 제약 이름·컬럼·값 조각이 클라이언트에 노출될 수 있었다. | Phase 4. `_log_db_error()` 로 연산·모델·예외 타입만 남기고 응답 detail 에서 원문 제거. 회귀: 응답 렌더링에 드라이버 문자열이 없음을 단언. |
| F-024 | LOW | §4 exists | Fix | **Fixed** | `exists()` 가 `COUNT(*)` 로 조건에 맞는 행을 끝까지 셌다. | Phase 4. `EXISTS` 로 교체해 첫 행에서 멈춘다. |
| F-025 | MED | ADR-004 / INV-10 (생성기 템플릿) | Fix | **Fixed** | `scripts/new_app.py` 의 dependency 템플릿이 **폐기된 패턴을 가르치고** 있었다: `yield` 뒤 commit(= FastAPI 상위 버전에서 응답 전송 후 실행되어 커밋 실패가 201 로 둔갑, blog 주석이 그래서 제거했다고 기록한 바로 그 패턴)과 deprecated alias `get_session`. `scripts/` 는 Phase 2 의 AST 스캔 범위(app/·tests/) 밖이라 빠져나갔다. | Phase 7. 템플릿을 조립 전용 + 정식 이름으로 교체. 회귀: `tests/scripts/test_new_app.py`(+2). |
| F-026 | HIGH | INV-5 / 계획서 §3 (모델 등록) | Fix | **Fixed** | `AppModule.import_models()` 가 `<package>.models` **패키지만** import 해서, 테이블 등록이 각 앱 `models/__init__.py` 의 재export 한 줄에 걸려 있었다. scaffold 가 만드는 빈 `__init__.py` 로는 모델이 조용히 미등록되고, 증상은 한참 뒤 "마이그레이션이 비어 있음"/"테이블이 안 생김" 으로만 나타난다. 실제로 catalog 생성 직후 이 상태였다. | Phase 7. 관례 모듈 `<package>.models.models` 도 함께 import(멱등). `iter_model_modules()` 기준과 일치시켜 runtime/Alembic 이 같은 집합을 본다. 회귀: `tests/core/test_registry_install.py`(재export 없는 앱도 등록됨). |
| F-027 | LOW | workflow-guide §2.1 (속성 명명) | Accept-out-of-scope | **Closed** | 지침서는 Dependency 인자와 Service/Repository 속성을 `db_session`/`self.db_session` 으로 규정하는데, 저장소 실제 코드는 `session`/`self.session` 이다. Phase 2 는 Dependency **함수 이름**만 정식화했고 속성 명명은 손대지 않았다. | Phase 9 에서 사용자 결정으로 **잔여 위험 이관**(R-011). 근거: 지침서 §2.1 에서 동작을 결정하는 것은 Dependency **함수 이름**(어떤 세션을 받는가)이고 그건 Phase 2 에서 정식화를 끝냈다. 남은 `session` vs `db_session` 은 내부 속성 표기이며, 92개 지점을 이름만 바꾸는 변경은 회귀 위험 대비 얻는 것이 없다. |
| F-028 | MED | INV-10 (생성기 CLI) | Fix | **Fixed** | `python -m scripts.new_app <name>` 이 앱을 정상 생성하고도 **종료 코드 1 로 죽었다.** 안내문의 em dash 를 Windows 한국어 콘솔(cp949)이 인코딩하지 못해 `UnicodeEncodeError` 가 났다. "성공했는데 실패로 보이는" 실패라 `new_app x && <다음 단계>` 조합이 조용히 끊기고, 원인은 스택트레이스를 읽기 전까지 드러나지 않는다. 실제로 이번 Phase 의 reports 생성에서 발생했다. | Phase 8. `__main__` 본문을 `main()` 으로 분리하고 진입점에서 `sys.stdout.reconfigure(errors="replace")`. 안내문은 사람이 읽는 부가 정보이므로 표현 못하는 글자는 대체하고 진행한다. 회귀: `tests/scripts/test_new_app.py`(cp949 스트림으로 `main()` 실행) — fail-on-revert 확인. |
| F-029 | MED | RAW-REP-003 / 지침서 §6 (정적 검사) | Fix | **Fixed** | 지침서는 "AST/정적 테스트로 f-string·문자열 연결과 비허용 `text()` 생성을 검출한다" 를 요구하는데 그런 검사가 없었다. Phase 6 은 Base 의 **런타임** 계약(named parameter, multi-statement 거부)만 세웠고, 조립된 SQL 이 `text()` 로 들어오는 것 자체는 리뷰에만 의존했다. Phase 8 에서 기능이 실제 SQL 을 소유하기 시작해 비로소 실효를 갖는 항목이다. | Phase 8. `tests/core/test_raw_sql_static_guard.py` 추가 — `app/`·`main.py`·`scripts/`·`migrations/` 를 AST 로 훑어 `text()` 인자가 리터럴 상수가 아니면 실패. 검출력 자체를 5종 위반 샘플로 검증하고, `Path.read_text` 오탐도 고정. `scripts/` 포함은 F-025 의 교훈이다. |
| F-030 | LOW | 계획서 §10 (MySQL 게이트 범위) | Fix | **Fixed** | MySQL 하네스의 `_EXPECTED_TABLES` 가 Phase 5 시점 5개 테이블에 머물러 있어, Phase 7 의 `catalog_products` 가 upgrade/downgrade 검증 대상에서 빠져 있었다. 신규 revision 의 downgrade 가 테이블을 남겨도 체인 테스트가 통과한다. | Phase 8. `catalog_products`·`sales_orders` 를 기대 집합에 추가. 이제 head→base→head 왕복이 두 예제 테이블까지 실제로 검사한다. |
| F-031 | MED | TEST-004 (게이트 재현성) | Fix | **Fixed** | 자식 프로세스를 띄우는 테스트가 출력 인코딩을 지정하지 않아 **결과가 실행 환경의 콘솔 설정에 좌우**됐다. `PYTHONIOENCODING=utf-8` 아래에서는 자식이 UTF-8 로 쓰고 부모가 로케일(cp949)로 읽어 디코딩이 실패하고, `stdout`/`stderr` 가 통째로 `None` 이 되어 12건이 깨졌다. 평소에는 양쪽이 우연히 같아서 통과했다. `test_admin_wiring.py` 는 `errors="replace"` 만 있어 죽지는 않지만 한글이 조용히 깨져 문자열 단언이 환경에 좌우됐다. | Phase 9. review_gate 로 전체 게이트를 돌리다 발견. 자식 env 의 `PYTHONIOENCODING` 과 부모의 `encoding=` 을 **양쪽 다** utf-8 로 고정. `PYTHONIOENCODING` 유무 두 조건에서 각각 613 passed 확인. |
| F-032 | HIGH | 계획서 §10 (공급망) | Fix | **Fixed** | lockfile 기준 취약점 검사가 없었고, 실제로 돌려보니 **4개 패키지에 알려진 취약점 12건**이 있었다(starlette 8, aiomysql 1, sqladmin 1, pytest 1). Bandit 은 소스만 보고 의존성 CVE 를 보지 않으므로 기존 게이트로는 영원히 드러나지 않는 종류다. | Phase 9. `pip-audit` 를 dev 의존성으로 추가하고 review_gate 의 `deps` 그룹으로 편입. starlette 1.6.0 · sqladmin 0.31.0 · aiomysql 0.3.2 · pytest 9.1.1(+pytest-asyncio 1.4.0) 로 상향해 **12 → 0**. 상향 후 전체 613 passed, MySQL 통합 28 passed 로 검증. 부수: `HTTP_422_UNPROCESSABLE_ENTITY` 폐기 대응. |
| F-033 | MED | 계획서 §10 (공급망) | Fix | **Fixed** | GitHub Action 이 이동 가능한 태그(`@v4`, `@v5`)로, MySQL 테스트 이미지가 태그(`mysql:8.4`)로 고정돼 있었다. 태그는 언제든 다른 커밋·다른 바이트를 가리킬 수 있고, 그 코드는 우리 CI 안에서 저장소와 토큰에 접근한다. | Phase 9. 두 Action 을 40자리 commit SHA 로, 이미지를 `@sha256:` digest 로 고정. 규칙은 `scripts/review_gate.py` 가 소유하고 취약 fixture 로 검출력을 증명한다(`tests/scripts/test_review_gate.py`). |
| F-034 | MED | 지침서 §2.1 / INV-10 (문서가 가르치는 이름) | Fix | **Fixed** | README 가 **폐기된 Dependency 별칭**을 19곳에서 가르치고 있었다. 특히 `get_session` 을 "쓰기용" 으로 설명하는데, Phase 2 이후 그 이름은 **동적 라우팅**(`get_routed_db_session`)의 별칭이다 — 문서를 따라 한 사람의 쓰기가 "명시적으로 승인된 특수 경로" 로 나간다. F-025 와 같은 계열이며, 문서는 어떤 코드 스캔 범위에도 들어 있지 않았다. | Phase 9. README 를 정식 이름으로 교체하고, `review_gate` 의 docs 그룹에 별칭 검사를 추가했다. 검사는 **가르치는 문서에만** 적용한다 — `docs/crp/` 는 무슨 일이 있었는지의 기록이라 옛 이름을 그대로 불러야 한다. 회귀: `tests/scripts/test_review_gate.py`(별칭 4종 + 경계 + README 실물). |
| F-035 | LOW | 2-2 위협 모델(문서 참조 무결성) | Fix | **Fixed** | `.github/workflows/ci.yml:1` 이 존재하지 않는 docs/crp/groups/fastapi-standard-restructure/charter.md (삭제된 그룹) 를 가리켰다. 그룹명이 바뀐 뒤 남은 잔재. | Round 13. 참조를 실재 charter 로 고치고, **근본 원인**인 검사 공백을 닫았다 — `check_doc_paths` 가 `.md` 확장자를 아예 보지 않았고 워크플로 파일도 검사 대상 밖이었다. 둘 다 넓혔고 기존 문서 오탐은 0건이었다. 회귀: `tests/scripts/test_review_gate.py` 2건. fail-on-revert 확인. |

<!--
규칙:
- 계약 위반만 Fix. 나머지는 Accept-out-of-scope(→ residual-risk.md) 또는 Wont-fix.
- Fix 는 회귀 테스트 + fail-on-revert 검증 후에만 Status=Fixed.
- Open 인 Fix 가 0건이어야 GATE 5 Done.
- Phase 6~8 의 Raw Base / catalog / reports 는 "미구현 신규 범위"이므로 결함이 아니라
  checklist 와 development-plan §10 에서 추적한다.
-->
